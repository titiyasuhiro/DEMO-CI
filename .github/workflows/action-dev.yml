name: Node CI

on:
  push:
    branches: [ develop ]
  pull_request:
    branches: [ develop ]

jobs:
    node-ci:
        name: Dev-CI
        runs-on: ubuntu-22.04
        if: "!contains(github.event.head_commit.message, '[skip ci]')"
        steps:
        - run: echo "Je suis en développement !"
